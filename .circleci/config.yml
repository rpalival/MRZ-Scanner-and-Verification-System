version: 2.1
jobs:
  build:
    docker:
      - image: cimg/python:3.11.0
    steps:
      - checkout
      - run: python3 MRTD.py
  test:
  docker:
    - image: cimg/python:3.11.0
  steps:
    - checkout
    - run: python3 MTTDtest.py

workflows:
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
